---
title: "Mercado Laboral"
subtitle: "Empleo y desempleo en Chile"
date: last-modified
---

```{python}
#| label: setup
#| include: false

import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

url = "https://raw.githubusercontent.com/Economia-y-politicas-publicas/chiledatos/main/data/trabajo.csv"

dt = pd.read_csv(url, sep=",", encoding="utf-8")
dt.columns = ["periodo", "total", "hombre", "mujer"]
dt[["total","hombre","mujer"]] = dt[["total","hombre","mujer"]].apply(pd.to_numeric, errors="coerce")

dt["año"] = dt["periodo"].str.extract(r"(\d{4})").astype(int)
dt = dt.sort_values("año").reset_index(drop=True)
dt["brecha"] = dt["mujer"] - dt["hombre"]

# Ticks: solo enero de cada año
mask_ene = dt["periodo"].str.contains("Ene")
ticks_idx = dt[mask_ene].index.tolist()
ticks_txt = dt[mask_ene]["año"].astype(str).tolist()

AUTOR = "Autor: Juan Miguieles Onetto | Fuente: INE"

def agregar_autor(fig):
    fig.update_layout(
        annotations=[dict(
            text=AUTOR,
            x=0, y=-0.15,
            xref="paper", yref="paper",
            showarrow=False,
            font=dict(size=10, color="gray"),
            xanchor="left",
        )]
    )
    return fig

eje_x = dict(
    tickmode="array",
    tickvals=ticks_idx,
    ticktext=ticks_txt,
    tickangle=0,
    tickfont=dict(size=12),
)
```

## 1. Tasa de desempleo total

```{python}
fig = px.line(
    dt, x=dt.index, y="total",
    markers=False,
    labels={"total": "Tasa de desempleo (%)", "x": "Año"},
    template="plotly_white",
    color_discrete_sequence=["#f39c12"],
)
fig.update_layout(
    title="Tasa de desempleo total en Chile (%)",
    font_family="Inter",
    xaxis=eje_x,
    yaxis=dict(tickformat=".1f"),
    margin=dict(b=80),
)
agregar_autor(fig)
fig.show()
```

## 2. Desempleo por sexo

```{python}
fig = go.Figure()
fig.add_trace(go.Scatter(
    x=dt.index, y=dt["total"],
    name="Total", line=dict(color="#f39c12", width=2),
))
fig.add_trace(go.Scatter(
    x=dt.index, y=dt["hombre"],
    name="Hombre", line=dict(color="#2980b9", width=2),
))
fig.add_trace(go.Scatter(
    x=dt.index, y=dt["mujer"],
    name="Mujer", line=dict(color="#e74c3c", width=2),
))
fig.update_layout(
    title="Tasa de desempleo por sexo (%)",
    yaxis_title="Tasa de desempleo (%)",
    xaxis=eje_x,
    template="plotly_white",
    font_family="Inter",
    legend=dict(orientation="h", y=-0.2, x=0.5, xanchor="center"),
    yaxis=dict(tickformat=".1f"),
    margin=dict(b=80),
)
agregar_autor(fig)
fig.show()
```

## 3. Brecha de desempleo mujer − hombre

```{python}
fig = px.area(
    dt, x=dt.index, y="brecha",
    labels={"brecha": "Brecha (pp)", "x": "Año"},
    template="plotly_white",
    color_discrete_sequence=["#8e44ad"],
)
fig.add_hline(y=0, line_dash="dash", line_color="gray", opacity=0.5)
fig.update_layout(
    title="Brecha de desempleo mujer − hombre (puntos porcentuales)",
    font_family="Inter",
    xaxis=eje_x,
    yaxis=dict(tickformat=".1f"),
    margin=dict(b=80),
)
agregar_autor(fig)
fig.show()
```