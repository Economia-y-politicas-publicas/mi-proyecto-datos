---
title: "Mercado Laboral"
subtitle: "Empleo y desempleo en Chile"
date: last-modified
---

```{python}
#| label: setup
#| include: false

import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

url = "https://raw.githubusercontent.com/Economia-y-politicas-publicas/chiledatos/main/data/trabajo.csv"

dt = pd.read_csv(url, sep=",", encoding="utf-8")
dt.columns = ["periodo", "total", "hombre", "mujer"]
dt[["total","hombre","mujer"]] = dt[["total","hombre","mujer"]].apply(pd.to_numeric, errors="coerce")

# Construir fecha desde el último mes del trimestre
meses_es = {
    "Ene":"01","Feb":"02","Mar":"03","Abr":"04",
    "May":"05","Jun":"06","Jul":"07","Ago":"08",
    "Sep":"09","Oct":"10","Nov":"11","Dic":"12"
}

def parsear_fecha(texto):
    partes = texto.strip().split()
    mes = partes[-2]   # último mes
    año = partes[-1]   # año
    mes_num = meses_es.get(mes, "01")
    return pd.to_datetime(f"{año}-{mes_num}-01")

dt = pd.read_csv(url, sep=",", encoding="utf-8")
dt["fecha"] = pd.to_datetime(dict(year=dt["Año"], month=dt["Mes"], day=1))
dt = dt.sort_values("fecha").reset_index(drop=True)

AUTOR = "Autor: Juan Miguieles Onetto | Fuente: INE"

def agregar_autor(fig):
    fig.update_layout(
        annotations=[dict(
            text=AUTOR,
            x=0, y=-0.15,
            xref="paper", yref="paper",
            showarrow=False,
            font=dict(size=10, color="gray"),
            xanchor="left",
        )]
    )
    return fig
```

## 1. Tasa de desempleo total

```{python}
fig = px.line(
    dt, x="fecha", y="total",
    markers=False,
    labels={"total": "Tasa de desempleo (%)", "fecha": "Año"},
    template="plotly_white",
    color_discrete_sequence=["#f39c12"],
)
fig.update_layout(
    title="Tasa de desempleo total en Chile (%)",
    font_family="Inter",
    xaxis=dict(dtick="M12", tickformat="%Y", tickangle=0, tickfont=dict(size=12)),
    yaxis=dict(tickformat=".1f"),
    margin=dict(b=80),
)
agregar_autor(fig)
fig.show()
```

## 2. Desempleo por sexo

```{python}
fig = go.Figure()
fig.add_trace(go.Scatter(
    x=dt["fecha"], y=dt["total"],
    name="Total", line=dict(color="#f39c12", width=2),
))
fig.add_trace(go.Scatter(
    x=dt["fecha"], y=dt["hombre"],
    name="Hombre", line=dict(color="#2980b9", width=2),
))
fig.add_trace(go.Scatter(
    x=dt["fecha"], y=dt["mujer"],
    name="Mujer", line=dict(color="#e74c3c", width=2),
))
fig.update_layout(
    title="Tasa de desempleo por sexo (%)",
    yaxis_title="Tasa de desempleo (%)",
    xaxis=dict(dtick="M12", tickformat="%Y", tickangle=0, tickfont=dict(size=12)),
    template="plotly_white",
    font_family="Inter",
    legend=dict(orientation="h", y=-0.2, x=0.5, xanchor="center"),
    yaxis=dict(tickformat=".1f"),
    margin=dict(b=80),
)
agregar_autor(fig)
fig.show()
```

## 3. Brecha de desempleo mujer − hombre

```{python}
fig = px.area(
    dt, x="fecha", y="brecha",
    labels={"brecha": "Brecha (pp)", "fecha": "Año"},
    template="plotly_white",
    color_discrete_sequence=["#8e44ad"],
)
fig.add_hline(y=0, line_dash="dash", line_color="gray", opacity=0.5)
fig.update_layout(
    title="Brecha de desempleo mujer − hombre (puntos porcentuales)",
    font_family="Inter",
    xaxis=dict(dtick="M12", tickformat="%Y", tickangle=0, tickfont=dict(size=12)),
    yaxis=dict(tickformat=".1f"),
    margin=dict(b=80),
)
agregar_autor(fig)
fig.show()
```